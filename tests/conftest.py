"""Shared pytest fixtures for radarsim-lab.

Populated as each phase lands (e.g. a seeded-RNG fixture once
`radarsim.core.rng` is implemented, a scenario-loading fixture once
`radarsim.io.scenario` is implemented).
"""
